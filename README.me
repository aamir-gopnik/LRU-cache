LRU cache Design

Functional Requirement.
getData(KEY)
addData(KEY,VALUE)

This code is limited to LRU cache.
Next Update : However, We can use a cache interface to broaden the usage
follow the Open/Closed SOLID principle.
